// login-api-test/src/styles/theme.ts

// Define um tema com cores para usar no styled-components
export const theme = {
  colors: {
    primary: '#b39ddb',       // Roxo pastel (cor principal)
    background: '#f5f0f8',    // Cor de fundo padrão
    white: '#fff',            // Branco
    gray: '#666'              // Cinza escuro para textos
  }
};
